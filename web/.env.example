# Backend that Vite's dev proxy forwards to.
# Override only if your Go server is bound to a non-default address.
VITE_API_PROXY=http://localhost:8080

# Drive the UI from MSW fixtures instead of the real backend.
# Useful for design review / Storybook-style screenshots.
# VITE_USE_MSW=true
