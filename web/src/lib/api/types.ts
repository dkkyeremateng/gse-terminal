/**
 * Hand-written API surface types.
 *
 * Will be replaced/augmented by `npm run gen:api`, which generates
 * `types-generated.ts` from /v1/openapi.json. Until that runs against the
 * live server, these are the types features import.
 */
export type {} // placeholder so this file is a module
