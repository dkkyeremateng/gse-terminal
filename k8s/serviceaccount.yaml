apiVersion: v1
kind: ServiceAccount
metadata:
  name: gse-app
  namespace: gse-terminal
  annotations:
    # GKE Workload Identity — bind to a GSA via:
    #   gcloud iam service-accounts add-iam-policy-binding \
    #     gse-app@PROJECT.iam.gserviceaccount.com \
    #     --role roles/iam.workloadIdentityUser \
    #     --member "serviceAccount:PROJECT.svc.id.goog[gse-terminal/gse-app]"
    iam.gke.io/gcp-service-account: "gse-app@teckdroids.iam.gserviceaccount.com"
automountServiceAccountToken: false
